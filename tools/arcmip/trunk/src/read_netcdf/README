
The program read_netcdf is a utility program that will read the
standard ARCMIP netcdf files.

The program requires the netCDF libraries, and can be compiled by
typing 'make' in the read_netcdf directory.

The user will need to edit the file include/domain.h to set the
size of the arrays contained in the ARCMIP netCDF files before
compiling this program.

If you have questions about this program please contact John Cassano
(cassano@cires.colorado.edu).

