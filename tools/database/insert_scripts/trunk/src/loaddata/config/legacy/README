These metadata files were created to load legacy data into emdac during the
genpro to netCDF format conversion project. They are unlikely to be needed
again so are not being maintained. If you need to use them, the load_data_proj
script will warn you about missing fields and add them.
