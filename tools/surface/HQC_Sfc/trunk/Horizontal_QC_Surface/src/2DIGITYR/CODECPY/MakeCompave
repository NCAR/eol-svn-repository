SHELL = /bin/sh

#CC=purify acc -g
#CC=acc -p
CC=cc
##CFLAGS = -g

compute_ave_var: compute_ave_var.o process_qcfrec.o
	$(LINK.c) -o compute_ave_var compute_ave_var.o process_qcfrec.o -l m
	chmod 0755 compute_ave_var

clean:
	rm *.o
