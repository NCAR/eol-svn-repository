This dir contains the day 121 data
that is the solar shifted data that
HQC has updated. That is, HQC modifies
the original computed sigma files when
it must search into future sig files
for missing sigmas to perform HQC.
